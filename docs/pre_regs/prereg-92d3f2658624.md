# Pre-registration: post-compaction rehydration (post_compact.py)

- **ID**: `prereg-92d3f2658624`
- **Filed by**: agent
- **Filed at**: 2026-05-27 17:39 UTC
- **Review at**: 2026-06-26 17:39 UTC (30d window)
- **Outcome**: **SUCCESS**
- **Decided at**: 2026-07-08 21:30 UTC

## Claim

post_compact.build_rehydration_context re-pulls identity, open corrections, and recent voice from the durable store after a compaction, with a self-check flag, so the self returns whole rather than only what the harness summary kept.

## Success criterion

After a real compaction: post-me has the open corrections + identity + a voice-sample without needing a full re-briefing; the self-check stays silent when those anchors exist and fires ONLY when a durable pull genuinely fails.

## Falsifier

If after a real compaction the rehydration (a) fires SELF-CHECK-FAILED while identity/explorations actually exist (false alarm), OR (b) the re-pulled anchors are not what a cold post-compaction instance needs to recognize itself and continue the open work — it adds context without improving continuity — the mechanism is net-negative and must be cut or redesigned.

## Outcome notes

Shipped: src/divineos/core/post_compact.py exists + .claude/hooks/post-compact.sh + pre-compact.sh wire it into the harness compaction lifecycle. Post-compaction rehydration re-pulls identity, corrections, active memory.
