# Pre-registration: Learn-time semantic dedup surface: divineos learn now queries find_similar_in_knowledge before filing and surfaces semantically-close existing entries informationally (caller chooses to supersede manually). Structural backing for kid ee96a4f7 (optimizer is DUMB) and kid 1d36be4f (MUST separate three layers — write-time check separated from storage path).

- **ID**: `prereg-50006560d988`
- **Filed by**: agent
- **Filed at**: 2026-06-11 19:48 UTC
- **Review at**: 2026-07-11 19:48 UTC (30d window)
- **Outcome**: **DEFERRED**
- **Decided at**: 2026-07-11 19:57 UTC

## Claim

When a learn call contains content that semantically matches existing entries above the dedup threshold, the CLI surfaces the close matches (kid prefix, similarity score, content snippet) before completing the fresh-file. --no-dedup-check disables the surface for scripted/batch writes. --dedup-threshold tunes the strictness.

## Success criterion

On a labeled benchmark of >=30 paraphrase-pair samples from the substrate, the surface correctly identifies >=80% of intentional paraphrases AND avoids surfacing >=90% of true non-duplicates at the default 0.70 threshold.

## Falsifier

If accuracy falls below 60% on either axis after threshold tuning, the surface is more noisy than useful and should be removed OR rethought as an audit-channel report instead of a write-time surface.

## Outcome notes

Deferring mid-drafting of the arm-inventory mechanism proposal with Aria. Learn-time semantic dedup surface deserves its own attention pass; naming here rather than rubber-stamping.
