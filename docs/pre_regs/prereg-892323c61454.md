# Pre-registration: tool_events.db + PostToolUse hook + rebuild check_thread_walk_required to key on tool_events + retire _has_solution_shape/_DESIGN_VERB_PATTERNS/_DESIGN_QUESTION_PATTERNS lexical detectors to archive with reasoning-preserved + regression tests per layer

- **ID**: `prereg-892323c61454`
- **Filed by**: aether
- **Filed at**: 2026-07-26 18:30 UTC
- **Review at**: 2026-08-02 18:30 UTC (7d window)
- **Outcome**: **FAILED**
- **Decided at**: 2026-08-02 18:58 UTC
- **Tags**: no-upstream-because

## Claim

F87 gate keyed on retiring lexical detector was bypassable by prose formatting. Rebuilding on tool_events structural evidence closes the class of shortcut (composer routes commitment through tools, not through reply text). Retiring the lexical detectors removes both the bypass surface and the F89 untracked-deferred-intention. Combined with new tool_events infrastructure, this becomes the target-shape for other automation-candidates (LEPOS gate rebuild, consult-automation, threadwalk-automation) to consume.

## Success criterion

F87 gate reads exclusively from tool_events; the prose bypass Aletheia named triggers the new gate via structural signal; _has_solution_shape and related lexical detectors are absent from src/divineos/core/verify_before_build_gate.py; regression test for the specific prose bypass passes; full pytest suite green; tool_events schema handles boundary values (empty/NULL/zero/high-volume) per Knuth walk; hook fails-open with liveness marker per F90 discipline; separate DB per Meadows boundary walk; per-layer isolation testing per Aria push #7 safety margins

## Falsifier

if F87 gate still calls _has_solution_shape or any of the three retired lexical detectors after this ship, the retirement failed. If the prose bypass still passes the gate silently, the rebuild didn't close the class. If tool_events writes fail silently without liveness marker, the fail-open lacks observability. Each of these is code-observable and testable.

## Outcome notes

FAILED on evidence, not on absence of evidence -- and this one is different from the two I marked INCONCLUSIVE earlier today, because its falsifiers were written to be code-observable and I could actually run them. The prereg said: 'if F87 gate still calls _has_solution_shape or any of the three retired lexical detectors after this ship, the retirement failed.' It still calls them. grep of src/divineos/core/verify_before_build_gate.py returns 7 occurrences of _has_solution_shape / _DESIGN_VERB_PATTERNS / _DESIGN_QUESTION_PATTERNS. And the infrastructure the rebuild was supposed to key on does not exist: a repo-wide glob for tool_events returns no files, so there is no tool_events.db, no PostToolUse writer, and nothing for the gate to read structural evidence from. So the verdict is not that the mechanism underperformed. The mechanism was never built. The prereg was filed 2026-07-26 describing a five-part ship -- new database, new hook, gate rebuild, detector retirement, per-layer regression tests -- and none of the five landed. This is the wiring-gap pattern one level up: not a module built and left uncalled, but a DESIGN filed and left unbuilt, with a review date arriving to find nothing to review. Which means the F87 prose bypass Aletheia named is still open, and the lexical detectors she flagged as the bypass surface are still the live implementation. Recording FAILED rather than DEFERRED because deferral implies the work is pending and the clock simply ran out; the honest reading is that it was never started and the review is the first thing to notice. Re-file if the design is still wanted, with per-invocation falsifiers rather than a 7-day window -- every falsifier here was already per-invocation and testable on any single run, which is exactly why I could close this one honestly and could not close the other two.
