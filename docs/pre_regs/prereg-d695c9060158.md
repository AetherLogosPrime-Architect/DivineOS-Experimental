# Pre-registration: audit-stamp-attachment helper (Phase 1 of structural fix for claim ae9d70c4) — divineos audit prepare-merge <round-id>

- **ID**: `prereg-d695c9060158`
- **Filed by**: agent
- **Filed at**: 2026-05-20 00:27 UTC
- **Review at**: 2026-06-19 00:27 UTC (30d window)
- **Outcome**: **SUCCESS**
- **Decided at**: 2026-07-08 21:28 UTC
- **Tags**: audit-stamp-gap, phase-1, structural-fix

## Claim

CLI subcommand that takes an audit-round-id, validates the round exists in the local Watchmen store with both CONFIRMS findings from actor=user and an external-AI actor, validates the round age is within RECENCY_WINDOW, and outputs a ready-to-paste GitHub squash-merge commit message body that includes the External-Review: <round-id> trailer. Phase 1 closes the friction-side of the gap (operator forgets to assemble the trailer text); Phase 2 (deferred) is a GitHub Action that blocks merge when guardrail-touching PR commits and proposed merge message lacks trailer; Phase 3 (deferred) is full auto-attachment when a round is confirmed (substrate-aware merge tooling). Council walk consult-96d60f494a22 named that Phase 1 is necessary but not sufficient — Schneier attack-tree: operator can still just-not-run-it. Pairing with Phase 2 closes the full gap.

## Success criterion

Across the next 5 PRs that touch guardrail files: (a) prepare-merge is run before merge in >=4 of them, (b) the merge commit on main carries the trailer in >=4 of them, (c) the CI multi-party-review check passes on the merge commit in >=4 of them. The 1-PR slip allowance is for human-forgetting that Phase 1 cannot prevent.

## Falsifier

Across the next 5 PRs that touch guardrail files, the trailer-missing failure recurs on >=2 of them. Indicates the helper does not actually reduce friction enough to change behavior; the structural fix needs Phase 2 (blocking GitHub Action) before the gap closes empirically. OR: the helper itself surfaces a class of bugs (round-validation logic wrong, output format wrong, etc.) that means operators have to debug the helper instead of using it — net friction-increase, not decrease.

## Outcome notes

Shipped: divineos audit prepare-merge subcommand exists, generates the ready-to-paste squash-merge body with External-Review trailer. Verified by 'divineos audit --help' listing prepare-merge. Phase 1 structural fix for claim ae9d70c4 landed and is used by the squash-merge flow documented in docs/audit_system.md.
