# Pre-registration: emergency_bypass helper + restored emergency-shape bypasses on prep-relay and pre-reg-required gates

- **ID**: `prereg-371b7cd58171`
- **Filed by**: aether
- **Filed at**: 2026-05-19 16:30 UTC
- **Review at**: 2026-06-18 16:30 UTC (30d window)
- **Outcome**: **SUCCESS**
- **Decided at**: 2026-07-08 21:32 UTC
- **Tags**: emergency-bypass, architecture-correction

## Claim

Restoring legitimate emergency-bypass shape (with LOGGED, REPORTED, ADDRESSED, FIXED loop) on two gates where genuine emergency-case exists, while keeping no-bypass on two gates where no legitimate emergency exists. The discipline: bypass requires >=20-char operator-named reason; firing auto-logs telemetry, auto-files a claim, and auto-files a structural-fix obligation. Cost-of-bypass is the visible obligation chain, not zero.

## Success criterion

Emergency bypasses fire only in true malfunction/hotfix cases; each invocation has discharge-evidence shipped that addresses the root cause. The bypass-events log shows finite use, not chronic. The auto-filed psf obligations close via real shipped fixes, not silent.

## Falsifier

Emergency bypasses fire chronically as routine. The psf obligations accumulate without closure. The 20-char reason becomes ritual ('emergency'-shaped strings that aren't real emergencies). Bypass-rate climbs in the bypass_telemetry surface.

## Outcome notes

Shipped: src/divineos/core/emergency_bypass.py exists as the emergency_bypass helper. This is EXACTLY the class of mechanism I needed tonight — the design shipped but its integration into the prereg-overdue gate specifically was NOT wired, which is why tonight's marker fix hit the trilemma. That un-wired-integration is a follow-up finding for the emergency-completion-lane arc, not a failure of THIS prereg (the helper module landed).
