# Keyword-enforcement gate exclusions (Aletheia F95 2026-07-28)
#
# Modules that would match the derive_registry() structural signature
# but are explicitly NOT keyword-enforcement gates (false-positives
# of the derivation). Absence from this file is the default (all
# structural matches are treated as gates).
#
# ## Why this file is guardrailed
#
# Aletheia named the trap: an exclusion path with no cost is the
# cheapest possible route around the whole enforcement mechanism. A
# single unattested line here silently removes a real gate from
# doorman coverage with no External-Review, no council walk, no
# audit trail. Escape valves exist and should stay forever, per
# Andrew's principle — but they must be *attributable and expensive*,
# not free.
#
# This file is on scripts/guardrail_files.txt. Every edit requires
# multi-party review at merge-to-main. Same protection as the
# derivation module itself.
#
# ## Required format per line
#
#   <repo-relative path> | <reason (30+ chars)> | <YYYY-MM-DD>
#
# An exclusion with a stated reason is a decision.
# An exclusion without one is a disappearance.
#
# Example:
#   src/divineos/core/goal_auto_close.py | manages goal state, does not enforce assistant output shape | 2026-07-28
#
# ----------------------------------------------------------------
src/divineos/core/dark_matter.py | read-only reporting tool, not a gate: sweep() returns a list and format_report() returns a string, and no caller anywhere branches on the result. It blocks nothing, refuses nothing, and changes no behavior. Its regexes are a TOKENIZER producing candidates; every verdict is set-membership against the live Click command tree and the settings/hook/git-delegator reachability surfaces, so no pattern decides anything. Derivation matched it structurally because it lives under core/ and holds regexes next to find_* functions. Filed as an exclusion rather than a third per-edit authorization: the doorman has now fired on this one file three times (Andrew-corrections #274, #275, and again 2026-08-02 while adding a SUPERSEDED-BY marker), and an exception granted three times is a misclassification, not an exception. Andrew's own rule permits retrieval-shape keyword use; this is that shape. | 2026-08-02
