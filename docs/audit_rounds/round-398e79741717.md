# Audit round: root-cause-audit: pre-push gates running checks uniformly without distinguishing the no-op/deletion case. Family: a pre-push gate applies its (expensive or strict) check across pushed refs without first asking whether the ref is a deletion, for which the check is vacuous. Survey: (1) check_multi_party_review.py ALREADY skips deletions per-ref (line 511, 'nothing to validate') — sibling that got it right; (2) check_push_readiness.sh pytest gate did NOT — ran full ~10min suite on deletion-only pushes — this is the laggard the fix aligns; (3) the root-cause-audit gate itself is naturally safe (deletion carries no commits to walk). Fix brings push-readiness into line with the multi-party gate's existing pattern.

- **ID**: `round-398e79741717`
- **Filed by**: aether
- **Filed at**: 2026-05-26 16:26 UTC
- **Tier**: WEAK
- **Findings**: 1

## Notes

No source ref (--no-source-ref used; round has no code substance).


## Findings

### Failure-family: pre-push gate runs its check uniformly without distinguishing the no-op/deletion case

- **ID**: `find-d6520831016b`
- **Actor**: aether
- **Severity**: LOW
- **Category**: ARCHITECTURE
- **Tier**: WEAK
- **Status**: RESOLVED

**Description**

Family: a pre-push gate applies its expensive/strict check across all pushed refs without first asking whether a ref is a deletion (all-zero local-sha), for which the check is vacuous. Sibling-instances surveyed: (1) check_multi_party_review.py line 511 — ALREADY skips deletions per-ref ('nothing to validate'); the sibling that got it right. (2) check_push_readiness.sh pytest gate — did NOT; ran full ~10min suite on deletion-only pushes; this is the laggard the fix corrects by aligning it to pattern (1). (3) root-cause-audit gate itself — naturally safe: a deletion carries no commits to walk. Conclusion: single laggard, now aligned to the existing correct sibling; no other instances found in the pre-push path.

**Resolution**

Verified: scripts/check_multi_party_review.py:522 has the deletion-skip comment ('Deletion (local_sha all zeros) — nothing to validate'). scripts/check_push_readiness.sh:62-104 has the full deletion-detection block ('Deletion-only push detection. Git pre-push protocol sends all-zero local-sha'). The laggard sibling has been aligned to the correct pattern from the family-audit finding.


---

_Exported from the local Watchmen store by `divineos audit export`. The store is runtime state and is not committed; this file is the portable record, readable without a local install._
