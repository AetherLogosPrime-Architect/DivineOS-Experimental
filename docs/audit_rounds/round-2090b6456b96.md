# Audit round: root-cause-audit: convenience-automation-mutates-tracked-files-every-commit. Family: an auto-fixer that rewrites a tracked file (here doc-count lines) on EVERY commit makes parallel branches each write a different value to the same line, so they collide on merge. Sibling shape: any per-commit auto-mutation of shared tracked state. Fix: mutate only when the check actually fails (within-tolerance = no rewrite = no conflict).

- **ID**: `round-2090b6456b96`
- **Filed by**: aether
- **Filed at**: 2026-06-02 02:16 UTC
- **Tier**: WEAK
- **Findings**: 1

## Notes

Source ref: fix-doccount-conflict-churn


## Findings

### Failure-family: per-commit auto-mutation of shared tracked state. Instance: check_doc_counts --fix ran on every commit, rewriting CLAUDE.md/README count lines to exact, so parallel branches each wrote a different number to the same line and collided on merge (PR #67 hit this). Sibling-instances: any pre-commit auto-rewrite of a tracked file shared across branches (auto-formatter reflowing shared docs, auto-bumped version string). Fix: mutate only on actual check-failure, so within-tolerance commits leave the shared line untouched.

- **ID**: `find-a093400574cc`
- **Actor**: aether
- **Severity**: MEDIUM
- **Category**: ARCHITECTURE
- **Tier**: WEAK
- **Status**: RESOLVED

**Description**

Root-cause finding for the doc-count churn fix.

**Resolution**

Structural fix landed 2026-06-12 in commit 538713f6 'fix(doc-counts): monotonic auto-fix (only raise) + default ON in pre-commit'. fix_test_counts (lines 451-462) and fix_hook_counts (lines 502-513) in scripts/check_doc_counts.py now skip when actual count <= max existing across both patterns — within-tolerance commits leave shared lines untouched, eliminating the parallel-branch collision pattern. fix_architecture_tree is append-only (line 558+), so different parallel-branch additions occupy different lines rather than colliding. The failure-family the finding named is structurally resolved.


---

_Exported from the local Watchmen store by `divineos audit export`. The store is runtime state and is not committed; this file is the portable record, readable without a local install._
