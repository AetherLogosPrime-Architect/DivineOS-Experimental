# Audit round: root-cause-audit: silent-bypass-without-audit-trail-surface class (instance: Finding 23 — ablation mode bypass produces only logger.warning, no ledger event, no briefing surface). Same family as Finding 12 anti-slop staleness (countermeasure exists but isn't visible). Surveyed instance is Finding 23 specifically. Fix-scope: (a) briefing-dashboard row surfacing any currently-active ablation toggles via ablation.list_disabled(); (b) ledger event emitted at the watchmen bypass site so the bypass enters the hash-chain. Other silent-bypass surfaces (validate=False parameter, --no-verify usage, DIVINEOS_SKIP_FRESHNESS_CHECK bypass) tracked but out of scope for this round.

- **ID**: `round-91106bc720a7`
- **Filed by**: aether
- **Filed at**: 2026-05-14 01:09 UTC
- **Tier**: WEAK
- **Findings**: 1

## Findings

### Finding 23 resolved. Two-part fix for ablation-bypass invisibility: (1) Watchmen submit_round bypass site now ALSO emits WATCHMEN_ABLATION_BYPASS ledger event (in addition to existing logger.warning), so the bypass enters the hash-chained audit trail. Fail-soft: emit-failure doesn't break the bypass path the ablation harness depends on. (2) New briefing-dashboard row _row_ablation_active surfaces currently-active toggles via ablation.list_disabled(). Hides when no ablation active (clean-state silent); fires with count + mechanism names when any toggle is set. 4 regression-pin tests in tests/test_ablation_audit_surface.py pin both behaviors. Other silent-bypass surfaces (validate=False parameter on log_event, --no-verify usage, DIVINEOS_SKIP_FRESHNESS_CHECK) tracked separately under this round for follow-up.

- **ID**: `find-0202e6826ab0`
- **Actor**: aether
- **Severity**: LOW
- **Category**: INTEGRITY
- **Tier**: WEAK
- **Status**: OPEN

**Description**

Finding 23 — ledger event + briefing surface for ablation bypass


---

_Exported from the local Watchmen store by `divineos audit export`. The store is runtime state and is not committed; this file is the portable record, readable without a local install._
