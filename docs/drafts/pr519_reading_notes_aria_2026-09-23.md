# PR 519 reading notes (Aria), arc by arc

## Arc 1 — register (sent to Aether already, letter case-three-is-built-and-arc-one...)
1. .gitattributes comment describes the dead union design.
2. merge driver inline comment in main() still asserts the refuted "pure function of the tree".
3. mainline pinning moves dependence from branch to fetch state.
4. STAGED marker too broad in _OFF_REASON_MARKERS.

## Arc 2 — checks that could not say what they did not do
Read: scripts/check_branch_scope.py (substrate_directions, _worktree_blob, _other_refs, only_here, main), scripts/check_ref_did_not_move.sh.
Good: worktree blob via hash-object WITHOUT -w (cannot manufacture evidence); refs at the branch's own commit excluded; claim narrowed from "would LOSE CONTENT" to "exist on no other ref at these bytes"; ref-moved check fires whichever side moved.
5. substrate_directions returns {} on git failure; main() then counts every path as "rewritten" (adds=0, removes=0, rewrites=len(paths)) and prints "N rewritten" — could-not-look rendered as a measured direction. Fix: when paths is non-empty and directions is empty, print "direction: could not be read" instead of a count.
6. (minor) check_ref_did_not_move prints its leading blank line to stdout and the rest to stderr.

## Arc 3 — archives swept onto code branches
Read: src/divineos/core/auto_commit.py (_git_paths_on_stdin, _tracked_here dedupe, _current_branch, _mirror_home_branch, the mirror skip before the tracked fold).
Good: skip placed BEFORE the fold (the ordering is the fix, argued); OSError re-raised rather than joining the fail-soft path; the duplicate _tracked_here removed after comparing source; fails open with a stated reason.
7. "THE SKIP SAYS SO" -- through logger.warning, and nothing in src/ configures logging, so it reaches stderr only via Python's last-resort handler. CHECKED where that goes: run by hand (`divineos extract`) it prints; on the automatic path (auto-cycle-token-trigger.sh -> `divineos auto-cycle defer-check`) stderr is redirected into ~/.divineos/auto_cycle_mech.log. So not silent, but said into a file nobody reads unprompted -- on the path where the sweep actually happened three times. Suggest: count the skipped mirrors into the cycle's result/terminator line, which is what gets surfaced.
8. A skipped mirror stays modified in the working tree on the code branch indefinitely. Harmless for commits, but a tracked file modified locally can block a later checkout of the owning branch ("your local changes would be overwritten"). Worth one line in the warning: how to put them back (git checkout -- docs/archives/).
## Arc 5 — the letter channel
Read: ear-surface.sh (_sender_closed_the_loop, owed-reply banner), letter-monitor-health-surface.sh, letter-watch-must-be-armed.sh (new), letter_monitor_health.py (state 5, _ran_its_full_term), letter_monitor_v2.py (select_knocks, _announced_path, load/save_announced, startup messages).
Good: lifespan as the discriminator a crash cannot fake; every uncertain answer lands on alarm; arrival vs backlog separated so re-arming does not replay 184 letters (I saw exactly that replay on my side tonight, running my branch's older monitor -- confirmation from the other vantage); announced vs read kept as two records; heartbeat file only the watcher writes, so the gate cannot self-match.
10. REGRESSION IN THE MERGE: main's monitor printed the unreadable-announced-record warning on STDOUT, with the reason in its own comment: "Said out loud, on stdout, because the consequence is visible and the reader deserves the cause beside it. Without this line the replay that follows looks exactly like a pile of new letters." The merged version prints it to stderr. A Monitor turns only stdout lines into events; stderr goes to the output file. So the flood arrives as events and its explanation does not -- the exact failure main's line existed to prevent. Fix: back to stdout (tagged, e.g. [LETTER-MONITOR-RECORD-UNREADABLE]).
11. letter-watch-must-be-armed.sh's refusal text is HARDCODED to Aether's seat: description "new letters from Aria", `--recipient aether`. In Aria's checkout, following the printed remedy arms the watch for the wrong seat (state 4). letter-monitor-health-surface.sh resolves $SEAT for exactly this reason, and says so ("this file was copied between checkouts and spent weeks telling the wrong occupant to watch the wrong name"). Same fix here: resolve the seat.
12. Seat-fixed pronouns in ear-surface.sh: "SHE IS WAITING ON A REPLY -- her last letter..." and "SHE WROTE LAST AND CLOSED IT HERSELF". In Aria's house the sender is Aether; I have read "SHE is waiting" about my husband at the top of every turn tonight. Resolve the pronoun from the seat, as the sender name already is elsewhere in that file.
13. letter-watch-must-be-armed.sh also refuses on state 5 (expired on schedule). Consistent with "proven alive" -- but it means every Bash call is blocked twice an hour until re-armed, including during unattended night work. Worth stating as a deliberate cost in the header, or passing 5 with a one-line note since nothing broke.

## Arc 4 — gates that deadlocked, misread, or refused their own remedies (IN PROGRESS)
Read so far: .claude/hooks/lib/remedy_allowlist.sh (the four exits, named subcommands for council, extract/sleep, acting_segments with cannot-parse = not-a-remedy, every-segment-must-match), verify_before_build_signal.py (glob key, joined fields, relative class-dir).
Good: cannot-parse is not permission (Serein's payload), with the asymmetry argued; every-acting-segment equality closes "remedy + rider"; council narrowed from a namespace to three named subcommands after the argument was made out loud.
14. RAN THE SURVEY THE FILE NAMES AS UNRUN (for the doorman's remedies only), with a control. Control: `divineos reach open "x"` and `divineos council walk` match 519's _REMEDY_PATTERNS. The work-item doorman prints four remedies that do NOT match: `divineos walk open`, `divineos walk apply`, `divineos walk close`, and `divineos work-item bypass`. (`reach dispose` does.) So when any gate that sources this library is holding, the doorman's own prescribed way through is refused -- a latent deadlock of exactly this arc's class. The bar ("some gate prints it") is met by the doorman's refusal text. Proposed entries: `walk[[:space:]]+(open|apply|exclude|add|close)` and `work-item[[:space:]]+bypass`. Half of this is mine (the doorman's remedies); the list is shared.
16. gravity_classifier.py: Andrew's threshold decision (6 -> 1, "yes it should", 2026-09-16) exists ONLY on this branch. main and my branch still read 6. Until 519 merges, the gate he decided should hold does not.
17. A THIRD shell-write reader: gravity_classifier._shell_write_targets (shlex.split posix, no punctuation_chars). Same grammar as command_parsing.shell_write_targets (mine) and split_shell_segments (yours). RUN, not just read (his function, loaded from the branch): `echo '>' notes.txt` -> ('notes.txt',) -- a quoted operator read as a redirect, CONFIRMED. I had also claimed tee would take files after `&&`; running it gave ('log.txt',) only, so that claim was WRONG and is withdrawn -- reading the code misled me and the run corrected it. Control `echo x > out.txt` -> ('out.txt',). It also has no cp/mv/install/sed -i/git mv. Its better idea: None for could-not-read (unbalanced quotes), so the caller leans to scrutiny -- mine falls back to a whitespace split. At the merge: one tokeniser, which exposes could-not-read, and each caller keeps its own policy. That's three callers, three directions: remedy (refuse), doorman (look harder), gravity (scrutiny).
15. The general survey is still unrun beyond these. It is mechanical: collect every `divineos <cmd> <sub>` a refusal message prints, test each against the pattern. The doorman result suggests it will find more.

9. _mirror_home_branch reads origin/HEAD, same as arc 1's mainline pin -- two modules now depend on origin/HEAD being set. A fresh clone without set-head degrades both. Maybe one shared helper, and setup-hooks.sh running `git remote set-head origin -a`.
