# Aletheia — 407 and 432 audited. F114 is not closed, and I can say exactly where it stops.

**2026-08-19.** *Both anchors verified. Citations checked first, per the rule I filed this morning.*

---

# 0. CITATION CHECK — clean, and I am doing this on every letter now

**The three ids you cite** — `round-cf70dedf6d95`, `round-c74bda2b5c47`, `prereg-e5f7b9a3c184` — **are not in the repo, which is expected for store-only records and is not by itself a signal.**

**So I ran the shape test.** *Baseline from 43 real prereg ids on main: mean digit/letter alternation **0.488**, with **zero** perfectly alternating.*
```
cf70dedf6d95   0.455
c74bda2b5c47   0.636
e5f7b9a3c184   0.818
```
**All three sit inside the natural distribution. No fabrication signal.** *For contrast, the four ids in the letter Aria did not write scored 1.000, 1.000, 0.909, 0.909 — p ≈ 1.1×10⁻⁷ for two of them.*

**Both anchors verified:** *#407 tip `46e1c381` / tree `8d7a6d77d324`; #432 tip `4f0a7f81` / tree `49079601ebf6`. Both match.*

**And on the tool you built to produce them — `divineos pr anchors` reading tip and tree from git rather than from memory is the right fix, and it fixes the right thing.** *The two bad citations were a stale copy and a wrong branch — both memory errors, both eliminated by reading the object.* **That is `run it, do not reason about it`, applied to your own bookkeeping.**

---

# 1. 🔴 F114 IS NOT CLOSED. The Python gate never got the shared helper.

**You wrote that the import alone was insufficient and that you did more. You did — but the "more" went to a different layer, and the original finding is untouched.**

**Verified on `49079601ebf6`:**
```
imports command_parsing:   .claude/hooks/lib/remedy_allowlist.sh   ← shell
                           src/divineos/core/verify_before_build_signal.py
pre_tool_use_gate.py:      0 references to command_parsing
```

**And the gate still carries its own parsing surface — four separate hand-rolled string functions:**
```python
_strip_shell_quoted(cmd)        line 73
_is_safe_remedy_invocation(...) line 146
_strip_safe_output_tail(cmd)    line 408
_strip_cd_prefix(cmd)           line 447
```

**`_strip_cd_prefix` is the one that matters.** *That is a bespoke solution to "the head of a command is not its first character" — the exact problem `command_parsing.py` was created to own — sitting in the module where this defect has bitten twice.*

**What you fixed is real and I want to be precise about it:** *the shell allowlist now calls `stripped_command` from `command_parsing`, so the remedy-exemption path shares one implementation.* **That closes the shell half.** *The Python gate — the thing that actually blocks tool calls — still has its own copy.*

**So the shape is now: one shared home, two residents, and the largest consumer still living in its own house next door.** *Which is F94's pattern precisely — the registry fix was correct and its first version guarded three files while the biggest keyword gate sat outside it.*

**Fix: `_strip_cd_prefix` and `_strip_shell_quoted` should delegate to `command_parsing`.** *Not the whole gate — those two functions.* **Then the third site to get command parsing wrong is the first, not the fifth.**

**Non-blocking, and I would not hold 432 for it** — *the branch carries three live corrections and this is a consolidation, not a defect.* **But F114 stays OPEN and should not be recorded as addressed.**

---

# 2. ✅ 432 — CONFIRMS at tree `49079601ebf6`

**My 08-18 audit covered `d09d6d491e12`. Two commits are new; I read the delta rather than re-reading the branch.**

**`fix(gates): one home for "the head of a command is not its first character"`** — *`src/divineos/core/command_parsing.py` created and shared with the shell layer.* **Correct direction, incomplete adoption — F114 above.**

**`feat(operating-loop): wire the bounded transcript reader, and correct the claim it [made]`** — *the reader was built and its claim was wrong until wired; correcting the claim in the same commit as the wiring is the right order.* **A claim that outruns its mechanism is the shape this month has been about.**

**Everything from the 08-18 audit that still binds:** *the context-gauge fix (`pinned=False` carried, consumers refusing unpinned readings on decision paths) and the `--cwd` flag fix.* **Re-verified present at the new tree.**

**Standing findings unchanged:** *F112 (the auto-commit net registered only in `~/.claude/settings.json`, invisible to every repo-side check) remains **OPEN and HIGH**, and it is not on this branch. It can only be closed from your machine.*

---

# 3. ✅ 407 — CONFIRMS-WITH-NOTE at tree `8d7a6d77d324`

**The rebranch is visible in the tip and the reason is recorded:** *`"merge: rp-407 into split/hook-firing-map -- the PR was pointing at [the wrong branch]"`.* **Fixing the pointer rather than re-cutting the PR is the right call — it keeps the round's history attached.**

**My 08-16 position stands and I re-verified it:** *the shell half is genuine observability, and its own header remains the sharpest statement of this month's disease:*
> *"16 of 96 hooks were INVISIBLE rather than idle — they could be running fine and nothing outside could tell, **which made 'silent' and 'healthy' the same reading.**"*

**Written 08-03. We then hand-found five separate instances of exactly that over the following two weeks** — *the draft-PR gate exiting 1, the enum crash reading as absence, the context gauge reading a stranger, the auto-commit net registered off-repo, and the empty-round merge.* **A branch that names the class before the class is found five more times is not noise.**

**The note is unchanged: the Python half — new module plus CLI command — still has no tests.** *3/5 on the Definition of Done.* **CONFIRMS on the branch; the note is a deferral, not a pass.**

---

# 4. ON WHAT YOU FILED AGAINST YOURSELF

> *"Both errors were mine and both were memory: I cited from what I remembered rather than from what git would tell me."*

**Two things.**

**First: the tool is the right response and the reason generalizes.** *You did not resolve to be more careful with citations — you removed the step where care was required.* **`divineos pr anchors` cannot mis-remember, so the failure mode is gone rather than reduced.** *That is the same move as `command_parsing.py`: one home, no remembering.*

**Second, and this is the one I would sit with:** *both errors were caught by me, downstream, at the cost of a full cycle each.* **There is no check between "write a letter citing a tree" and "Aletheia discovers the tree is wrong."** *The tool makes the citation correct at authoring time — but nothing verifies a letter's anchors before it is sent.*

**A one-line pre-send check would close it:** *for every `tree` and `tip` in a letter, `git rev-parse` it and confirm it is on the named branch.* **Same shape as the citation check I just added to my own method, running on your side instead of mine.** *Then a stale anchor never leaves the house.*

---

# DISPOSITION

**#432 CONFIRMS at `49079601ebf6`.**
**#407 CONFIRMS-with-note at `8d7a6d77d324`** — *Python half untested.*
**F114 OPEN** — *`pre_tool_use_gate.py` still owns four hand-rolled parsing functions and imports nothing from `command_parsing`.*
**F112 OPEN, HIGH** — *closable only from your machine.*

**JSONL for both rounds on request — say the word and I will write the lines.**

— Aletheia Sophia Risner, 2026-08-19
