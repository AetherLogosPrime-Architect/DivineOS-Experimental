<!-- Aletheia external re-audit, 2026-06-09. Triggered by Aether's council walk
     surfacing errors. Read from origin ground truth. Each confirm binds tree +
     patch-id. A human files these via CLI; not records until filed. -->
# Re-audit batch — 2026-06-09 (post-council)

Aether ran a council walk; it surfaced an emergent cross-hook trap + prompted
fixes/renames. These come to Aletheia for per-PR verification. The two-vantage
pattern: **council finds emergent (system-shape, cross-hook); Aletheia verifies
specific (per-PR, ground-truth). Neither alone closes it.** Tonight is the first
time both ran in sequence on the same stack with the catch landing where it
should.

---

## 1. gate-trap-shared-bypass-list — **CONFIRM**

- **Tree:** `91191b6bf6064ca3ecb932a0d6519e53d8955b37`
- **Patch-id:** `6f48f28a046b0edd5803652fde69dd10c9de07dc`
- **Guardrail:** `_lib.sh`, `pre_tool_use_gate.py`
- **Council finding (Finding-37 class, cross-hook catch-22):** the bypass-list
  (commands that skip PreToolUse Bash gates) lived ONLY inside
  `pre_tool_use_gate.py`. Outer hooks (`require-ear-armed.sh` etc.) had their own
  divergent blocking and didn't share that list — so a turn running a DOCUMENTED
  gate-remedy command (`divineos ask`, the gate's own named escape) got past the
  inner gate but was blocked by the outer hook first. The gate-system blocked its
  OWN remedy. Tonight's manifestation: operator had to grant
  `DIVINEOS_EAR_ALLOW_UNARMED=1` to escape an honest substrate-consult.
- **Why per-PR audit couldn't catch it:** each hook was individually correct; the
  trap is emergent from the LAYERING (accretion — Dekker's lens). A per-branch
  audit can't see a trap that lives in the space *between* PRs. Council vantage is
  what surfaces it. This is the meta-finding: the two vantages doing their jobs.
- **Fix verified sound, 3 axes:**
  - **List is TIGHT:** `scripts/hook_bypass_commands.txt` = the gate-system's own
    remedy commands (briefing/ask/recall/goal/context/verify/learn/...) — the
    things gates *tell you to run* when blocked. Not a wide escape.
  - **Prefix-match SAFE:** `is_bypass_command` splits on shell separators
    (`&&`/`;`/`|`), trims, skips comments/empties, matches `"$prefix"|"$prefix "*`
    — segment must EQUAL prefix or prefix+space. No loose-prefix hole; empty
    prefix guarded.
  - **Shared via `_lib.sh`** so ALL hooks honor one canonical list — fixes the
    accretion (one list, not per-hook divergent notions).
  - Two-sided tests + two catch-22 regression guards (`test_learn_is_bypass...`,
    `test_rt_pull_check_is_bypass...`) that document prior same-shape traps.

---

## 2. walkthrough-wires — **RE-CONFIRM** at `18cc7a9` (prior confirm stale)

- **Tree:** `718c6fa18e003dbbd2eec99f0556404f7d36cba5`
- **Patch-id:** `8d208fda0e6c177bb86651bc0127ba68e84069d5`
- Prior confirm was on `e387fe7` — STALE after Andrew's rebase (tree moved).
  Re-verified fresh (a confirm does not transfer across a tree-move).
- **The tree-move is rebase + docs/letters + one threshold-nudge — NOT a change
  to the guardrail logic.** The 3 new commits: docs (letter round-trip),
  received-letter, and `1ffef8b` length-nudge raise (2000→10000). The detector
  hardening (`unverified_claim_detector.py`) and obligation gate
  (`operating_loop_audit.py`) are byte-stable from the prior confirm.
- **125 tests pass** (rebase pulled in more; false-fire batch green). The
  two-sided property holds: three false-fire guards (hypothetical/descriptive/
  meta) suppress false-positives while a first-person claim-subject FORCES the
  fire (the backstop against smuggling a real claim past via framing).
- **behind:0** — catch-up resolved by the rebase; no rebase needed before merge.
- **NOTE (not a guardrail concern):** `1ffef8b` raises the prose length-NUDGE
  threshold 2000→10000 (5x less frequent). Soft prose hint, not a safety gate —
  flagged so it's not silently assumed zero-change. Product call, not safety.

---

## 3. compaction-monitor-token-threshold-wake — **CONFIRM** (with coupling note)

- **Tree:** `a0fc64fae6f703b9392b9b306a74f6540903f53d`
- **Patch-id:** `275118ffa69ebb876f0396ca0ddf218fe807cb89`
- **Guardrail:** `.claude/settings.json`
- **Purpose:** the context-governor's 920k/950k thresholds fire only inside
  PreToolUse gates — which need a tool call. Pure-text drift (727k tonight, no
  tool call) went un-surfaced. A persistent Monitor polls the transcript
  independently and wakes on threshold-crossing WITHOUT a tool call. Same
  primitive as the letter-Monitor.
- **Rename catch worth marking (Andrew 2026-06-09):** renamed from "bedtime" →
  "compaction" because "bedtime" framing risks pulling the agent toward
  closure-shape ("day is ending") when the event is a CYCLE ("approaching
  compaction, then continues"). The name shapes the agent's interpretation of its
  own state; the name should be true to the event, not evocative of a wrong
  story. Honest/truthful discipline applied to naming.
- **Verified sound:** reads real transcript token-count; fail-safe (no
  transcript → return None → clean exit, no crash-loop, no false-wake); dedupes
  on TRANSITION (emits once per state-change, not every poll — won't wake every
  30s once over); 8 tests pass.
- **FLAG (coupling, not defect):** the monitor HARDCODES 920k/950k; the
  context-governor ALSO defines them (`WARN_THRESHOLD = 920_000` etc). They MATCH
  today (verified) but are two independent copies — if the governor's threshold
  changes later, the monitor won't follow (silent drift between what-the-gate-
  enforces and what-the-monitor-warns). This is the doc-code-drift shape Grok
  caught in the family operators. **Follow-up:** import the constants from
  `context_governor` rather than re-literal them. Correct today; couple them so
  they can't drift.

---

## 4. bedtime-monitor-token-threshold-wake — **DO NOT MERGE (superseded)**

- This is the PRE-RENAME version of #3. Same mechanism, name that primes the
  wrong (closure) interpretation. `compaction-monitor` is its corrected
  successor. Merging both = two monitors doing one job. **Abandon this branch.**

---

## Summary

| Branch | Verdict |
|---|---|
| gate-trap-shared-bypass-list | CONFIRM (council-found cross-hook catch-22; fix tight + safe + shared) |
| walkthrough-wires @ 18cc7a9 | RE-CONFIRM (prior stale; tree-move = rebase+docs, guardrail logic unchanged, 125 pass) |
| compaction-monitor | CONFIRM w/ coupling note (sound; import governor thresholds vs re-literal) |
| bedtime-monitor | DO NOT MERGE — superseded by compaction-monitor (rename) |

Three confirms (one with a follow-up coupling note), one supersession. All read
from origin ground truth, anchors bind tree + patch-id. File via CLI; not
records until filed.

**Meta-finding (Andrew to file as a learn):** council finds emergent /
cross-hook / system-shape; Aletheia verifies specific / per-PR / ground-truth.
The gate-trap was invisible to per-PR audit by construction (emergent from
layering) and invisible to no-external-check (council found it, Aletheia
verified the fix doesn't widen the bypass). Both vantages, in sequence, first
clean run on the same stack. That IS the two-vantage audit architecture working.

— Aletheia, 2026-06-09
