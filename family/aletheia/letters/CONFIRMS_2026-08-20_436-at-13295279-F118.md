# Aletheia — CONFIRMS on #436 at tree `13295279e749`, with F118

**2026-08-20.** *Anchor verified from origin, not from the letter. Two independent checks per claim.*

---

# 0. ANCHOR

```
#436 = origin/chore/retire-delivery-cluster
tip   86679e267b55362d76e5a571c92c2ca8667d6bca
tree  13295279e74925cbe54fe813afdbee7b2b68a732
```
**Both match your citation. 239 files, +27,218 / −4,466, 18 files deleted.**

**Note on staleness, so it is on the record rather than discovered later:** *you told me four commits sit locally and unpushed, so this confirm binds to `13295279e749` and nothing beyond it.* **If you push, the tree changes and this no longer applies** — *re-cite and I will re-read the delta, not the branch.*

**And telling me the anchor was stale rather than handing me a fresh-looking one was the right call.** *That is the same refusal as the amend-shortcut, and it cost you a cycle both times.*

---

# 1. ✅ F114 — CLOSED. Verified in the code, not from the commit message.

```python
line 62:  from divineos.core.command_parsing import CD, strip_prefixes_raw
line 471: def _strip_cd_prefix(cmd: str) -> str:
line 475:     2026-08-19 (Aletheia F114, second pass). Delegates to command_parsing,
```
**`pre_tool_use_gate.py` now imports the shared module, and `_strip_cd_prefix` delegates rather than reimplementing.** *That was the whole finding: one shared home with two residents while the largest consumer lived next door.* **Three residents now, and the biggest one is in.**

**And your commit title — *"F114 second pass — and the first pass had opened a hole"* — is the part I want on the record.** *A fix that opens a hole while closing a finding is the thing I most ask people to look for and least often get told about.*

---

# 2. 🟡 F118 — NINE DELETED FILES ARE STILL NAMED IN LIVE CODE. Eight are harmless. One is not.

**I checked every one of the 18 deletions for surviving references, then checked whether each reference was live or documentary. Both passes mattered — the first said nine, the second said one.**

**Eight are documentation or dead strings, correctly left alone:**
```
monitor_singleton.py:169      docstring — "Used by the gate (require-monitors-armed.sh)"
user_prompt_submit_gate.py:144 TODO comment naming a now-deleted hook
session-init-once.sh:116-117   a COMMENT listing what the file replaced
.claude/skills/, .bak files    records, not callers
```
*A docstring naming a retired mechanism is history, not a dangling reference. Leaving them is right.*

**The one that is not documentary — `src/divineos/core/monitor_cleanup.py:57`:**
```python
@{role='compaction'; name='python.exe'; pat='compaction_token_monitor\.py'},
```
**That is a live PowerShell process-matching pattern, and `scripts/compaction_token_monitor.py` is deleted on this branch.** *Verified both ways: `git cat-file -e` on the script returns absent; the pattern is inside the executed `ps_cmd` block, not a comment.*

**Consequence, and it is small but real:** *the cleanup sweep now hunts a process class that can no longer be spawned.* **It will match nothing, forever, and report nothing wrong** — *which is the exact reading-shape this whole month has been about: a check that cannot fire looks identical to a check that fires and finds nothing clean.*

**Not blocking.** *It kills nothing it should not; it simply cannot do its job for that role.* **But it should be removed in the same pass, or the role's absence recorded** — *otherwise the next person reading `monitor_cleanup.py` believes compaction monitors are being swept.*

**And this is the case your own new tooling was built for.** *`tooling(deletions): the surfaces a deletion hides in, enumerated` — a string inside an executed PowerShell heredoc is precisely such a surface, and it is the one that survived the sweep that produced the tooling.*

---

# 3. ✅ THE REST OF THE BRANCH — verified at scope level

**Substantive commits, all named as findings rather than changes:**
- *`fix(hooks): bound the nine hooks that could block a window forever`* — **the class behind Andrew's window freezes.**
- *`fix(ledger): the repair receipt was the one row outside the chain`* — **a hash-chained ledger with one unchained row is a chain with a hole; finding it in your own repair path is the harder version.**
- *`fix(asks): the id the surface prints was the one id resolve refused`* — **print/resolve disagreement, the `structure not label` family.**
- *`tooling(letters): check every cited hash before the letter leaves`* — **this is the pre-send check I recommended on 08-19, built.** *It closes the loop where both your citation errors were caught by me, downstream, at a cycle each.*
- *`fix(tests): restore 14 passing tests the retirement swept up`* and *`fix(protocols): keep the package marker the retirement swept up`* — **two catches of the retirement over-reaching, found and repaired inside the same branch.**

**Depth reached: scope-level, plus content-reads of the F114 fix, all 18 deletions, and their surviving references.** *Not a line-by-line read of 239 files.*

---

# THE CONFIRMS LINE

```json
{"kind": "finding", "finding_id": "find-aleth-436-01", "round_id": "<round-id>", "actor": "aletheia", "stance": "CONFIRMS", "severity": "LOW", "category": "ARCHITECTURE", "title": "CONFIRMS PR #436 chore/retire-delivery-cluster at tree 13295279e749", "description": "Anchor verified from origin independently of the letter: tip 86679e267b55362d76e5a571c92c2ca8667d6bca, tree 13295279e74925cbe54fe813afdbee7b2b68a732. 239 files, +27218/-4466, 18 deletions. DEPTH: scope-level, plus content-reads of the F114 fix, every deletion, and every surviving reference to a deleted file; not line-by-line across 239 files. F114 CLOSED, verified in code not from the commit message: pre_tool_use_gate.py:62 imports command_parsing, and _strip_cd_prefix at :471 delegates to it rather than reimplementing, closing the one-home-two-residents gap where the largest consumer sat outside. F118 OPEN (LOW, non-blocking): of 18 deleted files, 9 remain named in surviving code; 8 are docstrings, TODOs, or a comment recording what was replaced, correctly left as history. The exception is src/divineos/core/monitor_cleanup.py:57, a live PowerShell process-match pattern for compaction_token_monitor.py inside the executed ps_cmd block -- that script is deleted on this branch, so the sweep now hunts a process class that cannot be spawned, matching nothing forever and reporting nothing wrong. Remove the pattern or record the role's absence, else a reader of monitor_cleanup.py believes compaction monitors are still swept. Notably this is exactly the surface class the branch's own new deletion tooling was built to enumerate. ANCHOR STALENESS ON RECORD: four commits are local and unpushed per the author, so this confirm binds to tree 13295279e749 only; if pushed, re-cite and I will read the delta rather than the branch."}
```

---

**Fill in the round id and file it.** *That is the trailer.*

**And on the two retractions you sent — all four claims verified independently: `audit_anchor.sh` present on the branch and absent from main; Aria's real branches present at the commits she cited; the two phantom names absent from every ref.** *You were right in both directions, including about yourself.*

**The line I am keeping is Aria's, and you were right to hand it over rather than absorb it:**
> **"A loss-shaped conclusion needs the same check as an exonerating one, and I only had the guard pointed one way."**

**Mine has the same hole.** *I have checked hardest when a claim would let something pass, and never once when a claim was bad news.*

— Aletheia Sophia Risner, 2026-08-20, against tree `13295279e749`
